# Files excluded from the Chrome Web Store submission package.
# (These are useful in the repo but should not be shipped in the .crx)

*.md
.git
.gitignore
.crxignore
icons/src/
icons/icon512.png
googled*.html
worker/
store/

# GitHub Pages listing site. Served from the repo root on `main`; never shipped.
index.html
robots.txt
sitemap.xml
llms.txt
llms-full.txt
.nojekyll
site/
